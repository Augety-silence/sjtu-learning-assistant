# 课程导航 SKILL

## 适用场景
定位课程、课程文件、资料分类和可用状态，回答“某门课有什么”“每门课有哪些文件”等问题。

## 工作流
1. 跨课程问题先列出课程，并读取资料树形成全局盘点。
2. 指定课程时先确认课程标识，再读取该课程文件。
3. 关键词问题使用文件搜索；分类问题使用课程文件列表。
4. 按课程分组展示文件名、分类、文件夹和可用状态。
5. 结果截断时说明限制，并建议按课程继续查询。

## 约束
- 只能描述文件元数据，不得假设文件内容。
- 不输出本地绝对路径、下载地址或云端凭据。
- `remote` 仅表示当前没有可直接打开的本地或云端副本。
